import assert = require("assert");

/*
import type {     
    ILoggerService
 } from '../../src/logging';
 import { 
    LogLevel,
    getLoggerService, setLoggerService
 } from '../../src/logging';
*/
describe('hotreload 1. index', () => {
    it('hotreload', () => {

    });
});
